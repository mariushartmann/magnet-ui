import React from "react";
import { ICheckboxProps } from "./checkbox.types";
export declare const Checkbox: {
    ({ className, disabled, error, hint, id, label, mode, name, onChange, onClick, ref, rules, style, theme, value }: ICheckboxProps): JSX.Element;
    displayName: string;
};
declare const MagnetCheckbox: React.MemoExoticComponent<{
    ({ className, disabled, error, hint, id, label, mode, name, onChange, onClick, ref, rules, style, theme, value }: ICheckboxProps): JSX.Element;
    displayName: string;
}>;
export { MagnetCheckbox };
