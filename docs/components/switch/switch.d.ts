import React from "react";
import { ISwitchProps } from "./switch.types";
export declare const Switch: {
    ({ className, disabled, error, hint, id, label, mode, name, onChange, onClick, ref, rules, style, theme, value }: ISwitchProps): JSX.Element;
    displayName: string;
};
declare const MagnetSwitch: React.MemoExoticComponent<{
    ({ className, disabled, error, hint, id, label, mode, name, onChange, onClick, ref, rules, style, theme, value }: ISwitchProps): JSX.Element;
    displayName: string;
}>;
export { MagnetSwitch };
