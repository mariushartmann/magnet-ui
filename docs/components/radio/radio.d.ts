import React from "react";
import { IRadioProps } from "./radio.types";
export declare const Radio: {
    ({ checked, className, disabled, error, id, label, name, onChange, onClick, ref, style, theme, value }: IRadioProps): JSX.Element;
    displayName: string;
};
declare const MagnetRadio: React.MemoExoticComponent<{
    ({ checked, className, disabled, error, id, label, name, onChange, onClick, ref, style, theme, value }: IRadioProps): JSX.Element;
    displayName: string;
}>;
export { MagnetRadio };
