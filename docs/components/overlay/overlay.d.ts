import React from "react";
import { IOverlayProps } from "./overlay.types";
export declare const Overlay: {
    ({ absolute, className, color, id, onClick, opacity, style, value, zIndex }: IOverlayProps): JSX.Element;
    displayName: string;
};
declare const MagnetOverlay: React.MemoExoticComponent<{
    ({ absolute, className, color, id, onClick, opacity, style, value, zIndex }: IOverlayProps): JSX.Element;
    displayName: string;
}>;
export { MagnetOverlay };
